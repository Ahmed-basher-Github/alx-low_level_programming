#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strcmp - a function that compares two strings 
 * @s1: the first string
 * @s2: the other string
 * Return: int + || - || 0
 */
int _strcmp(char *s1, char *s2)
{
	int p = strcmp(s1, s2);

	return (p);

	


}
