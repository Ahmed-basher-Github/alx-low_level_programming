#include "main.h"
/**
 * add - a functoin that adds two numbers
 * @x: the first number entered by user
 * @y: the second number entered by user
 * Return: type int the sum of the two numbers
 */
int add(int x, int y)
{
	return (x + y);
}
