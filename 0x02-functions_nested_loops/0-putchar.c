#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Return: Always return 0 (Success)
 */
int main(void)
{
	const char message[] = "_putchar";

	printf("%s\n", message);
	return (0);
}
